using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class SetAveragePriceText : MonoBehaviour
{
    public TMP_Text text;
    public WeeklyFinances wf;
    List<string> texts;
    public void SetTexts()
    {
        //Debug.Log(wf.TrackerRecord.Count);
        //for(int i=0; i<wf.TrackerRecord.Count; i++)
        //{
        //    //Debug.Log("PROBA PROBA");
        //    //texts.Add(wf.TrackerRecord[i].products[0].Price.ToString());
        //    //text.text = texts.ToString();
        //}
        
    }
}
